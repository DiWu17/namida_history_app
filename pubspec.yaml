name: namida_history_app
description: "Namida listening history visualizer."
publish_to: 'none'
version: 1.1.3+5

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  file_picker: ^10.3.10
  fl_chart: ^0.68.0
  path_provider: ^2.1.5
  provider: ^6.1.5+1
  archive: ^4.0.4
  metadata_audio: ^0.7.0
  path: ^1.9.0
  sqlite3: ^2.9.3
  permission_handler: ^11.3.1
  android_intent_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true
